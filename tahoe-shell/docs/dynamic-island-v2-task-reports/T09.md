# T09: Settle swipe directly to its target

Status: ready-to-commit
Base SHA: `cc9f4692b740053ff03162be98084321dfaa369d`
Started: 2026-07-15
Review round 1: REQUEST_CHANGES (no-media right-enter width 400→360)
Review round 2: APPROVE (`019f63d5-4164-7b22-ada5-efef335f6875`)

## Goal

Eliminate two-phase collapse-then-expand on swipe settle. Gesture resolve computes target scene + geometry once; settle holds that target; settle end only clears gesture phase.

## Exact allowlist

- `tahoe-shell/services/DynamicIsland.qml`
- `tahoe-shell/components/DynamicIslandOwnership.js`
- `tahoe-shell/tests/test_dynamic_island_v2_expected_failures.py`
- `tahoe-shell/tests/test_dynamic_island_swipe_settle.py`
- `tahoe-shell/docs/dynamic-island-v2-task-reports/T09.md`

## Forbidden

- Zeroing swipeProgress during settle (resting-width jump)
- Cross-session click suppression
- Breaking vertical reject / diagonal dead band

## Implementation

- Pure `resolveSwipeSettle` + `swipePreviewWidthFor` in DynamicIslandOwnership.js
- `resolveSwipe` keeps target progress; never zeros on enter
- No-media right-enter uses summary progress (-1 → 360) matching expanded_summary
- Production `swipePreviewWidth` delegates to pure helper
- `beginSwipe` records start forcedState; cancel restores; clearSwipe clears start
- Settle timer only clears gesture phase after animation
- T02 T09 xfail → GREEN

## Tests

```text
full tahoe-shell/tests → 558 passed, 184 subtests (0 xfailed)
```
