# T13: Replace the OSD scene

Status: review
Base SHA: `5e6c460acb44130d8e89cc64edf81db05ad4ec8b`
Started: 2026-07-15

## Goal

Replace percentage + circular ring OSD with icon + horizontal progress + exact value.
Keep volume/brightness event behavior; continuous updates must not replay content enter.

## Exact allowlist

- `tahoe-shell/components/DynamicIslandOsdView.qml` (new)
- `tahoe-shell/components/DynamicIslandContent.qml`
- `tahoe-shell/components/DynamicIslandOverlay.qml` (wire only if needed for accent/theme)
- `tahoe-shell/services/DynamicIsland.qml` (OSD presentation mapping only)
- `tahoe-shell/tests/test_dynamic_island_osd_scene.py` (new)
- `tahoe-shell/tests/test_dynamic_island_brightness_zero.py`
- `tahoe-shell/tests/test_dynamic_island_volume_osd_dedupe.py`
- `tahoe-shell/tests/test_tahoe_symbol_migration.py` (OSD ring layout contract → V2 scene)
- `tahoe-shell/docs/dynamic-island-v2-task-reports/T13.md`

## Forbidden

- PipeWire / backlight direct reads
- Copying Controls state into island ownership
- Keeping Canvas ring
- Danger red for muted without product reason
- Second OSD service
- git add -A / force push

## Implementation plan

1. New `DynamicIslandOsdView.qml`: 20px TahoeSymbol, 120px bar, right value (muted → 静音 / 0).
2. Content host: replace osdRow + Canvas with OsdView; opacity only on scene active (not on every progress tick).
3. Progress width Behavior only for value changes.
4. Service: clamp progress 0–1; muted progress 0; brightness 0 valid (already T05).
5. Tests for layout contract, no Canvas ring, rapid update path, 0/1/50/100, muted.

## Tests

```text
git diff --check
module: osd_scene + brightness_zero + volume_osd_dedupe + public contracts
full tahoe-shell/tests
```


## Review round 1

Staged SHA-256 recorded at review time (not embedded here to avoid self-hash churn).

## Implementation summary

1. New DynamicIslandOsdView: 20px TahoeSymbol, 120px horizontal bar, 40px value.
2. Content host replaces Canvas ring with OsdView; opacity only on osdActive.
3. Service presentOsdEntry clamps 0–1, muted → 0 + 静音 value text; brightness 0 valid.
4. Overlay forwards accentColor/darkMode for bar accent.
5. Symbol migration test updated for V2 OSD scene.

## Tests

```text
git diff --check → clean
module osd_scene + brightness + volume dedupe + contracts + arbitration → passed
full tahoe-shell/tests → 605 passed, 185 subtests
```
