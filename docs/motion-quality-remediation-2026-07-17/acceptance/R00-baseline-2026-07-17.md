# R00 · 基线锁定 · 2026-07-17

## 测试全量

`cd tahoe-shell && pytest tests/ -x -q` → **747 passed, 217 subtests passed in 26.40s**（全绿，无 skip 异常）。

## niri validate

`niri validate -c config/niri/tahoe-phase0.kdl` → `config is valid`。

## 玻璃 guardrail

`scripts/check-tahoe-glass-guardrails.sh` → passed（24 PanelWindow namespace / 4 TahoeGlassRegion / 22 regions 文件 / Phase 5 popup 几何动画检查全过）。

## 嵌套会话冒烟

`NIRI_MODE=nested bash scripts/run-tahoe-session.sh`（25s timeout）：niri 嵌套启动、quickshell 加载、TahoeGlass region set/commit 正常（17 次 commit），SIGTERM 干净退出。日志无 QML TypeError/ReferenceError。日志存 /tmp/r00-nested-smoke.log。

## 量化 grep 四项基数（components/*.qml）

| 指标 | 基数 |
| --- | --- |
| `Behavior on` 总计数 | **124** |
| press token 覆盖文件数（pressScale/Motion.press） | **10** |
| `Transition {` 使用文件数 | **0** |
| 零 Behavior 文件数 | **27**（与 01-research-report §5 清单一致） |

## 四个点名现象基线描述（复现步骤）

1. **剪贴板弹窗（#1–#5）**：打开剪贴板弹窗 → 全列表瞬时出现无过渡；refresh 触发整列表 delegate 重建闪烁；删除/固定条目瞬跳无动画；行无 hover/press 反馈。
2. **WiFi 弹窗（#7–#10）**：打开 WiFi 弹窗点"重新扫描"→ 列表瞬间清空塌缩、"未发现网络"占位闪现、扫完整表重建；面板高度跳变；开着 30s 定时 rescan 周期性闪烁；PSK 输入框 0↔42 高度瞬切。
3. **菜单（#13–#19）**：点菜单项 → 先等 2×70ms 闪烁再发 activated，动作滞后明显；行按压缩放突兀；MenuPopup 确认卡高度二值瞬切；AppMenuPopup 每开必整列表重建。
4. **灵动岛（#20–#29）**：场景换场"全隐-换-全现"出现空胶囊帧；morph 期玻璃 region 8px 量化边缘阶梯；换场中输入死区；通知 compact↔expanded 整层眨眼；OSD 首现瞬跳（v2OsdEnterMs=0）。

## 结论

基线锁定完成。允许仅提交 acceptance + 状态表（本任务无产品代码改动）；本提交同时首次纳入整个 docs/motion-quality-remediation-2026-07-17/ 驱动器目录。
