# T14: Replace compact notifications

Status: review
Base SHA: `fcc62c7791fad84e533820ab7121fc2ed492b995`
Started: 2026-07-15

## Goal

- Real app identity (icon URL + appName + summary + body).
- Content-driven width/height for compact notifications.
- Replacement in-place (no re-enter).
- Body click = default action (frozen for T15).
- Swipe dismiss uses stable notification ID.
- No second notification model/queue.

## Exact allowlist

- `tahoe-shell/components/DynamicIslandNotificationView.qml` (new)
- `tahoe-shell/components/DynamicIslandContent.qml`
- `tahoe-shell/components/DynamicIslandOverlay.qml`
- `tahoe-shell/services/DynamicIsland.qml` (notification presentation mapping)
- `tahoe-shell/tests/test_dynamic_island_notification_compact.py` (new)
- `tahoe-shell/tests/test_dynamic_island_notification_identity.py`
- `tahoe-shell/tests/test_dynamic_island_v2_surface.py` (notification geometry helpers)
- `tahoe-shell/docs/dynamic-island-v2-task-reports/T14.md`

## Forbidden

- Island notification model copy
- dbus-monitor
- Second notification queue
- Changing org.freedesktop.Notifications owner
- T15 expand chevron / action buttons (later)
- git add -A / force push


## Review round 1

Staged SHA-256 recorded at review time (not embedded here to avoid self-hash churn).

## Implementation summary

1. DynamicIslandNotificationView: app icon URL (safe schemes), appName, summary, body, critical edge.
2. Service presentation fields + iconUrlFor/urgency from Notifications; replace-id in-place.
3. Body click → invokeNotificationDefaultAction (default); swipe dismiss by stable id.
4. Overlay short/long geometry (300/420 x 60/80).
5. No second notification model.

## Tests

```text
full tahoe-shell/tests → 614 passed, 185 subtests
```
