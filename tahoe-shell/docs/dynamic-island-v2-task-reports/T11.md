# T11: Install the unified V2 surface

Status: review
Base SHA: `a8e614c2819663881a8b4f29f161dfee14efe1d8`
Started: 2026-07-15

## Review round 1

Verdict: REQUEST_CHANGES (`019f6403-fc40-7832-88a7-dcc318e774e0`)

Fixes:
- Document intentional media hard-cut (visualizer Timer gated on visible); enter-only opacity Behavior.
- Clamp max capsule height against screen height − top inset − margin.
- T11 report status → review.

## Goal

Keep the single Overlay PanelWindow and TahoeGlass pill region, but install V2 surface geometry, dark-focus fill/stroke, Loader-based scene host (current + outgoing only), and document the 1-region island recipe in material governance. Business scene chrome (media buttons, OSD ring, etc.) stays functional; full scene redesign is T12+.

## Exact allowlist

- `tahoe-shell/components/DynamicIslandOverlay.qml`
- `tahoe-shell/components/DynamicIslandContent.qml`
- `tahoe-shell/services/DynamicIsland.qml` (resting/swipe width table only — must match Overlay surface widths)
- `tahoe-shell/docs/tahoe-material-governance.md`
- `tahoe-shell/tests/test_tahoe_material_governance.py`
- `tahoe-shell/tests/test_dynamic_island_v2_surface.py`
- `tahoe-shell/tests/test_motion_token_convergence.py` (radius / geometry assertions)
- `tahoe-shell/tests/test_dynamic_island_swipe_settle.py` (mid-band width numbers for settle preview)
- `tahoe-shell/tests/tst_dynamic_island_media_hit_testing.qml` (expanded media geometry coordinates)
- `tahoe-shell/tests/tst_dynamic_island_media_interaction_lifecycle.qml` (Loader.item media lookup)
- `tahoe-shell/tests/tst_dynamic_island_visualizer_animation_align.qml` (Loader non-instantiation gate)
- `tahoe-shell/docs/dynamic-island-v2-task-reports/T11.md`

## Forbidden

- Second Overlay / V2 PanelWindow
- Eighth material or material profile strength changes
- SpringAnimation on glass region geometry
- Production shell.qml wiring changes
- DynamicIslandTheme.js
- Replacing media/OSD/notification business layouts (T12–T16)
- git add -A / force push

## Implementation

1. Overlay: V2 mid-band width/height from IslandMotion; expanded radius capped 28–32; compact top inset 4; screen margin clamp; SettingsTheme island fill/stroke/text; 1px stroke; single pill GlassPanel region (no Spring on geometry).
2. Content: Loader hosts for expanded media + summary with exit-hold unload; compact/transient remain lightweight always-on chrome; no dual always-instantiated expanded scenes.
3. Service: restingWidthForState + swipeRightWidth locked to Overlay mid-band so swipe settle preview matches surface.
4. Governance: DynamicIsland 1-region pill recipe in material table + tests.
5. Tests: new surface suite; hit-testing / visualizer / lifecycle QML harnesses updated for Loader.item and mid-band geometry.

## Tests

```text
git diff --check → clean
bash scripts/check-tahoe-glass-guardrails.sh → passed
T11 module suite (surface + governance + motion + swipe + media hit/lifecycle/visualizer + public contracts + expected failures) → 115 passed, 95 subtests
full tahoe-shell/tests → 586 passed, 185 subtests
```

## Runtime / visual

- Functional scenes still present via production Overlay path.
- Expanded outline no longer height/2 ellipse (cap 30 within 28–32).
- Fill uses island semantic tokens (~80–87% deep neutral) so pill glass can show through.
- Hidden/non-expanded outputs do not instantiate MediaView/SummaryView.
